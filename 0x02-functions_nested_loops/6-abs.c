#include "main.h"
/**
  *_abs - Computes the absolute value of integer
  *@n: Integer to check
  *
  *Return: absolute value of integer
  */
int _abs(int)
{
	int n;

	if (n >= 0)
		return (n);
	else
		return (-n);
}
